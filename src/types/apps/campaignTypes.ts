export type CampaignType = {
  id: number
  title: string
  no_of_leads: number
  active: boolean
}

export type CampaignClientType = {
  name: string
  title: string
  subject: string
  body: string
}
